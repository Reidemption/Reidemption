#Container options
"""
unique items only
duplicates allowed
unordered
User Ordered
Autosorted

For the student list assignment we need unordered unique
Python list is user ordered, duplicates allowed
"""
#Creation of an ADT with the features that we need
#UnorderedUniqueContainer.py
class ADT:
    def Insert(self,item):
        #return False if duplicate

    def Delete(self,item):
        #return False if not found

    def Retrieve(self, item):
        #return the item or return None

    def Size(self):
        #returns the how many items there are
    
    def Exists(self, item):
        #returns False if the item doesn't exist

 